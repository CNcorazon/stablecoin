# web3auth-pnp-examples/docker-compose.yml
version: '3.8'

services:
  # 后端服务（仅内部访问）
  backend:
    build:
      context: ./deploy-contracts
      dockerfile: Dockerfile
    container_name: xd-backend
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - PORT=3000
    env_file:
      - ./deploy-contracts/.env
    volumes:
      - ./deploy-contracts/logs:/app/logs
    networks:
      - app-network

  # 前端服务（带API代理）
  frontend:
    build:
      context: ./quick-starts/react-quick-start
      dockerfile: Dockerfile
    container_name: xd-frontend
    restart: unless-stopped
    ports:
      - "80:80" # 只暴露前端端口
    depends_on:
      - backend
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
